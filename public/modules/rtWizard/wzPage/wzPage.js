export default class wzPage {
    constructor() {
        this.template = require('./wzPage.html')
        this.bindings = {
            prev: '&?',
            next: '&?'
        }
    }

    controller() {

    }
}